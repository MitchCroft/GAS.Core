namespace MC.GAS.Config {
    /**
     * The lookup collection of configuration entries that can be retrieved for processing
     */
    export type ConfigurationCollection = MC.GAS.Mapping<ConfigurationEntry>;
}
